import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Smart_library_mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
